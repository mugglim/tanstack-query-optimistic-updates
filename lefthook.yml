pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,jsx,ts,tsx}"
      run: pnpm exec eslint {staged_files}
    prettier:
      glob: "*.{js,jsx,ts,tsx}"
      run: pnpm exec prettier --check {staged_files}
